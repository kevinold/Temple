<h1>Listing pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Published</th>
    <th>Version</th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%=h page.title %></td>
    <td><%=h page.published %></td>
    <td><%=h page.version %></td>
    <td><%= link_to 'Preview', page, :target => '_blank' %></td>
    <td><%= link_to 'Edit', edit_admin_page_path(page) %></td>
    <td><%= link_to 'Destroy', [:admin, page], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New page', new_admin_page_path %>
