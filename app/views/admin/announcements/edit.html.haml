- title "Edit Announcement"

= render :partial => 'form'

%p
  = link_to "Show", admin_announcement_path(@announcement)
  |
  = link_to "Back", admin_announcements_path
  = link_to 'Delete', [:admin, @announcement], :confirm => 'Are you sure?', :method => :delete
