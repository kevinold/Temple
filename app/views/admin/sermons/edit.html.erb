<h1>Editing sermon</h1>

<% form_for([:admin, @sermon]) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', [:admin, @sermon] %> |
<%= link_to 'Back', admin_sermons_path %>
