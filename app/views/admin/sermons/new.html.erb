<h1>New sermon</h1>

<% form_for([:admin, @sermon]) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_sermons_path %>
